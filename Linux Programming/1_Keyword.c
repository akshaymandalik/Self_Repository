int main(){

    return 0;
}

/*
    returning from the main function is 
    equivalent to exit() since the run-time function which 
    calls the main function calls the exit() function internally 
    with returned value.
    
*/

